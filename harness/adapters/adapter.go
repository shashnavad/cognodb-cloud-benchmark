// STATUS: NOT IMPLEMENTED
// Purpose: the GraphDBAdapter interface every target implements —
// Connect, IngestBatch, PointLookup, FilteredLookup, Traversal(hops),
// Aggregation, ExecuteWrite, Close.

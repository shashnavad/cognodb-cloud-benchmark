// STATUS: NOT IMPLEMENTED
// Purpose: the fixed query set run against every adapter — point lookup,
// filtered lookup, 1/2/3-hop traversal, degree aggregation, mixed 80/20 write.
// Cypher only (Kuzu, Bolt targets, FalkorDB's GRAPH.QUERY all accept Cypher).
